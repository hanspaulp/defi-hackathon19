import React from "react";
import { Grid, Paper, Typography } from "material-ui";

const style = {
  Paper: { padding: 20, marginTop: 10, marginBottom: 10 }
};

export default ({ exercises }) => <Grid item />;
